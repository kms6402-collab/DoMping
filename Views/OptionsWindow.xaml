<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="DoMping.Views.OptionsWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Title="Options" UseLayoutRounding="True" Height="540" MinHeight="500" Width="580" MinWidth="580" WindowStartupLocation="CenterScreen" ResizeMode="CanResize" Background="#0a0f0c" xmlns:views="clr-namespace:DoMping.Views;assembly=DoMping" SourceInitialized="Window_SourceInitialized">
  <FrameworkElement.Resources>
    <ResourceDictionary>
      <Style x:Key="{x:Type TextBlock}" TargetType="{x:Type TextBlock}">
        <Setter Property="FontSize" Value="12" />
        <Setter Property="FontWeight" Value="Regular" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="HorizontalAlignment" Value="Left" />
        <Setter Property="Foreground" Value="#ffffff" />
      </Style>
      <Style x:Key="OptionSubHeaderTextStyle" TargetType="{x:Type TextBlock}">
        <Setter Property="Margin" Value="0,0,0,4" />
        <Setter Property="Foreground" Value="#33ff99" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="FontFamily" Value="Cascadia Mono, Consolas" />
      </Style>
    </ResourceDictionary>
  </FrameworkElement.Resources>
  <Grid Margin="0">
    <Grid.RowDefinitions>
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <TabControl TabStripPlacement="Left" HorizontalContentAlignment="Left">
      <TabItem Name="GeneralTab" Header="General">
        <DockPanel Margin="20,0" LastChildFill="False">
          <TextBlock DockPanel.Dock="Top" Text="Probe Options" Style="{StaticResource OptionHeaderTextStyle}" />
          <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,10,0,0">
            <TextBlock Text="Ping interval:" Width="110" />
            <TextBox Name="PingInterval" Width="60" MaxLength="5" PreviewTextInput="NumericTextbox_PreviewTextInput" />
            <ComboBox Name="PingIntervalUnits" Width="100" Margin="10,0,0,0" Style="{StaticResource {x:Type ComboBox}}">
              <ComboBoxItem Content="seconds" IsSelected="True" />
              <ComboBoxItem Content="minutes" />
              <ComboBoxItem Content="hours" />
            </ComboBox>
          </StackPanel>
          <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,10,0,0">
            <TextBlock Text="Ping timeout:" Width="110" />
            <TextBox Name="PingTimeout" Width="60" MaxLength="2" PreviewTextInput="NumericTextbox_PreviewTextInput" />
            <TextBlock Text="seconds" Margin="10,0,0,0" />
          </StackPanel>
          <Separator DockPanel.Dock="Top" Height="1" Background="#223028" Margin="0,20,0,5" />
          <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,10,0,0">
            <TextBlock Text="Alert threshold:" Width="110" />
            <TextBox Name="AlertThreshold" Width="60" MaxLength="2" PreviewTextInput="NumericTextbox_PreviewTextInput" />
            <TextBlock Text="dropped pings" Margin="10,0,0,0" />
          </StackPanel>
          <TextBlock DockPanel.Dock="Top" Text="Startup" Margin="0,25,0,10" Style="{StaticResource OptionHeaderTextStyle}" />
          <TextBlock DockPanel.Dock="Top" Text="Choose how DoMping should start:" Margin="0,0,0,5" />
          <ComboBox Name="StartupMode" DockPanel.Dock="Top" Width="200" Style="{StaticResource {x:Type ComboBox}}">
            <ComboBoxItem Content="Blank (default)" Tag="Standard" IsSelected="True" />
            <ComboBoxItem Content="List input window" Tag="Standard" />
            <ComboBoxItem Content="Load a favorite" Tag="Favorite" />
          </ComboBox>
          <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,13,0,0">
            <FrameworkElement.Style>
              <Style>
                <Setter Property="UIElement.Visibility" Value="Collapsed" />
                <Style.Triggers>
                  <DataTrigger Value="Standard" Binding="{Binding ElementName=StartupMode, Path=SelectedItem.Tag}">
                    <Setter Property="UIElement.Visibility" Value="Visible" />
                  </DataTrigger>
                </Style.Triggers>
              </Style>
            </FrameworkElement.Style>
            <StackPanel Margin="0,0,30,0">
              <TextBlock Text="Probe count:" Margin="0,0,0,4" />
              <TextBox Name="InitialProbeCount" Width="60" HorizontalAlignment="Left" MaxLength="2" PreviewTextInput="NumericTextbox_PreviewTextInput" />
            </StackPanel>
            <StackPanel>
              <TextBlock Text="Column count:" Margin="0,0,0,4" />
              <TextBox Name="InitialColumnCount" Width="60" HorizontalAlignment="Left" MaxLength="2" PreviewTextInput="NumericTextbox_PreviewTextInput" />
            </StackPanel>
          </StackPanel>
          <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,13,0,0">
            <FrameworkElement.Style>
              <Style>
                <Setter Property="UIElement.Visibility" Value="Collapsed" />
                <Style.Triggers>
                  <DataTrigger Value="Favorite" Binding="{Binding ElementName=StartupMode, Path=SelectedItem.Tag}">
                    <Setter Property="UIElement.Visibility" Value="Visible" />
                  </DataTrigger>
                </Style.Triggers>
              </Style>
            </FrameworkElement.Style>
            <StackPanel Margin="0,0,35,0">
              <TextBlock Text="Select a favorite:" Margin="0,0,0,4" />
              <ComboBox Name="InitialFavorite" DockPanel.Dock="Top" Width="250" Style="{StaticResource {x:Type ComboBox}}" />
            </StackPanel>
          </StackPanel>
        </DockPanel>
      </TabItem>
      <TabItem Name="AdvancedTab" Header="Advanced">
        <DockPanel Margin="20,0" LastChildFill="False">
          <TextBlock DockPanel.Dock="Top" Text="Advanced ICMP Options" Style="{StaticResource OptionHeaderTextStyle}" />
          <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,10,0,0">
            <TextBlock Text="Time to live:" Width="110" />
            <TextBox Name="TTL" Width="80" MaxLength="3" PreviewTextInput="NumericTextbox_PreviewTextInput" />
            <TextBlock Text="hops" Margin="10,0,0,0" />
          </StackPanel>
          <Separator DockPanel.Dock="Top" Height="1" Background="#223028" Margin="0,20" />
          <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0">
            <RadioButton Name="PacketSizeOption" GroupName="IcmpData" Content="Packet data size" IsChecked="True" Width="160" Checked="PacketSizeOption_Checked" />
            <TextBox Name="PacketSize" Width="80" MaxLength="5" Visibility="{Binding IsChecked, ElementName=PacketSizeOption, Converter={StaticResource BooleanToHiddenVisibilityConverter}}" PreviewTextInput="NumericTextbox_PreviewTextInput" TextChanged="PacketData_TextChanged" />
            <TextBlock Text="bytes" Margin="10,0,0,0" Visibility="{Binding IsChecked, ElementName=PacketSizeOption, Converter={StaticResource BooleanToHiddenVisibilityConverter}}" />
          </StackPanel>
          <DockPanel Dock="Top" Margin="0,7,0,0">
            <RadioButton Name="UseCustomPacketOption" GroupName="IcmpData" Content="Custom packet data" DockPanel.Dock="Left" VerticalAlignment="Top" Width="160" Checked="UseCustomPacketOption_Checked" />
            <TextBox Name="PacketData" Height="80" DockPanel.Dock="Right" VerticalContentAlignment="Top" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" MaxLength="65500" Visibility="{Binding IsChecked, ElementName=UseCustomPacketOption, Converter={StaticResource BooleanToVisibilityConverter}}" TextChanged="PacketData_TextChanged" />
          </DockPanel>
          <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" HorizontalAlignment="Left" Margin="20,15,0,0">
            <TextBlock Foreground="#5f7d6c" Text="Total packet size:" />
            <TextBlock Name="Bytes" Foreground="#33ff99" Margin="6,0,4,0" Text="64" />
            <TextBlock Foreground="#33ff99" Text="bytes" />
          </StackPanel>
          <Separator DockPanel.Dock="Top" Height="1" Background="#223028" Margin="0,20" />
          <DockPanel Dock="Top" LastChildFill="False">
            <CheckBox Name="DontFragment" DockPanel.Dock="Left" Content="Don't fragment" />
          </DockPanel>
        </DockPanel>
      </TabItem>
      <TabItem Name="ServicePortsTab" Header="Service Ports">
        <DockPanel Margin="20,0" LastChildFill="False">
          <TextBlock DockPanel.Dock="Top" Text="Service Port Checks" Style="{StaticResource OptionHeaderTextStyle}" />
          <TextBlock DockPanel.Dock="Top" TextWrapping="Wrap" Margin="0,10,0,0" Text="Prefix a hostname with S/ (for example S/10.0.1.10) to check the TCP and UDP ports below in a single probe. Each port is reported individually as OPEN or CLOSED." />
          <Separator DockPanel.Dock="Top" Height="1" Background="#223028" Margin="0,20" />
          <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,0">
            <TextBlock Text="TCP ports:" Width="110" />
            <TextBox Name="ServiceTcpPorts" Width="260" MaxLength="200" ToolTip="Comma-separated TCP port numbers" />
          </StackPanel>
          <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,15,0,0">
            <TextBlock Text="UDP ports:" Width="110" />
            <TextBox Name="ServiceUdpPorts" Width="260" MaxLength="200" ToolTip="Comma-separated UDP port numbers" />
          </StackPanel>
          <TextBlock DockPanel.Dock="Top" TextWrapping="Wrap" Margin="0,15,0,0" Opacity="0.7" Text="Example: 22,23,80,56" />
        </DockPanel>
      </TabItem>
      <TabItem Name="PopupAlertsTab" Header="Notifications">
        <DockPanel Margin="20,0" LastChildFill="False">
          <DockPanel Dock="Top" LastChildFill="False">
            <TextBlock DockPanel.Dock="Left" Text="Popup Alerts" Style="{StaticResource OptionHeaderTextStyle}" />
          </DockPanel>
          <RadioButton Name="PopupsDisabledOption" DockPanel.Dock="Top" GroupName="PopupAlerts" Content="Disabled" Margin="0,15,0,0" />
          <RadioButton Name="PopupsMinimizedOption" DockPanel.Dock="Top" GroupName="PopupAlerts" Content="Show when minimized" Margin="0,6" />
          <RadioButton Name="PopupsAlwaysOption" DockPanel.Dock="Top" GroupName="PopupAlerts" Content="Always show" IsChecked="True" />
          <DockPanel Dock="Top" LastChildFill="False" Margin="0,20,0,5">
            <CheckBox Name="IsAutoDismissEnabled" DockPanel.Dock="Left" Content="Auto-dismiss notifications" IsChecked="True" />
          </DockPanel>
          <StackPanel DockPanel.Dock="Top" Margin="50,0,0,0" Orientation="Horizontal" Visibility="{Binding IsChecked, ElementName=IsAutoDismissEnabled, Converter={StaticResource BooleanToVisibilityConverter}}">
            <TextBox Name="AutoDismissInterval" Width="60" MaxLength="2" PreviewTextInput="NumericTextbox_PreviewTextInput" />
            <TextBlock Text="seconds" Margin="10,0,0,0" />
          </StackPanel>
        </DockPanel>
      </TabItem>
      <TabItem Name="EmailAlertsTab" Header="Email">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
          <DockPanel Margin="20,0,20,15">
            <TextBlock DockPanel.Dock="Top" Text="Email Alerts" Style="{StaticResource OptionHeaderTextStyle}" />
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,15,0,15">
              <CheckBox Name="IsEmailAlertsEnabled" DockPanel.Dock="Top" Content="Enable email alerts" IsChecked="True" Click="IsEmailAlertsEnabled_Click" />
            </StackPanel>
            <DockPanel LastChildFill="False" Dock="Top" Margin="0" Visibility="{Binding IsChecked, ElementName=IsEmailAlertsEnabled, Converter={StaticResource BooleanToVisibilityConverter}}">
              <FrameworkElement.Resources>
                <ResourceDictionary>
                  <Style x:Key="{x:Type TextBlock}" TargetType="{x:Type TextBlock}" BasedOn="{StaticResource {x:Type TextBlock}}">
                    <Setter Property="HorizontalAlignment" Value="Right" />
                    <Setter Property="Margin" Value="0,0,12,0" />
                  </Style>
                  <Style x:Key="{x:Type DockPanel}" TargetType="{x:Type DockPanel}">
                    <Setter Property="Margin" Value="0,3,0,3" />
                    <Setter Property="Dock" Value="Top" />
                  </Style>
                </ResourceDictionary>
              </FrameworkElement.Resources>
              <TextBlock DockPanel.Dock="Top" Text="Server Information" Style="{StaticResource OptionSubHeaderTextStyle}" />
              <DockPanel>
                <Grid Width="120">
                  <TextBlock Text="SMTP server" />
                </Grid>
                <TextBox Name="SmtpServer" />
              </DockPanel>
              <DockPanel>
                <Grid Width="120">
                  <TextBlock Text="Port" />
                </Grid>
                <TextBox Name="SmtpPort" HorizontalAlignment="Left" MaxLength="5" Width="75" PreviewTextInput="NumericTextbox_PreviewTextInput" />
                <CheckBox Name="IsSmtpSslEnabled" Content="Enable SSL/TLS" FontSize="13" Margin="50,0,0,0" />
              </DockPanel>
              <CheckBox Name="IsSmtpAuthenticationRequired" DockPanel.Dock="Top" Content="This server requires authentication" Foreground="#5f7d6c" FontSize="13" Margin="120,8,0,0" Click="IsSmtpAuthenticationRequired_Click" />
              <StackPanel DockPanel.Dock="Top" Visibility="{Binding IsChecked, ElementName=IsSmtpAuthenticationRequired, Converter={StaticResource BooleanToVisibilityConverter}}">
                <DockPanel>
                  <Grid Width="120">
                    <TextBlock Text="Username" />
                  </Grid>
                  <TextBox Name="SmtpUsername" />
                </DockPanel>
                <DockPanel>
                  <Grid Width="120">
                    <TextBlock Text="Password" />
                  </Grid>
                  <PasswordBox Name="SmtpPassword" />
                </DockPanel>
              </StackPanel>
              <TextBlock DockPanel.Dock="Top" Text="Email Addresses" Margin="0,12,0,5" Style="{StaticResource OptionSubHeaderTextStyle}" />
              <DockPanel>
                <Grid Width="120">
                  <TextBlock Text="Recipient address" />
                </Grid>
                <TextBox Name="EmailRecipientAddress" LostFocus="EmailRecipientAddress_LostFocus" />
              </DockPanel>
              <DockPanel>
                <Grid Width="120">
                  <TextBlock Text="From address" />
                </Grid>
                <TextBox Name="EmailFromAddress" />
              </DockPanel>
              <Button Name="TestEmailButton" Style="{StaticResource ButtonStandardStyle}" DockPanel.Dock="Top" Width="85" Margin="0,18,0,0" Content="Test" HorizontalAlignment="Left" Visibility="{Binding IsChecked, ElementName=IsEmailAlertsEnabled, Converter={StaticResource BooleanToVisibilityConverter}}" Click="TestEmail_Click" />
            </DockPanel>
          </DockPanel>
        </ScrollViewer>
      </TabItem>
      <TabItem Name="AudioAlertTab" Header="Sounds">
        <DockPanel Margin="20,0">
          <TextBlock DockPanel.Dock="Top" Text="Audio Alerts" Style="{StaticResource OptionHeaderTextStyle}" />
          <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,15,0,0">
            <CheckBox Name="IsAudioDownAlertEnabled" Content="Enable audio alert: host down" IsChecked="True" Click="IsAudioDownAlertEnabled_Click" />
          </StackPanel>
          <Grid DockPanel.Dock="Top" Margin="30,10,0,0" VerticalAlignment="Top" Visibility="{Binding IsChecked, ElementName=IsAudioDownAlertEnabled, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel>
              <TextBlock Text="Audio file path" />
              <TextBox Name="AudioDownFilePath" Margin="0,4,0,8" />
              <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <Button Style="{StaticResource ButtonStandardStyle}" Width="85" Content="Browse..." Click="AudioDownBrowse_Click" />
                <Grid>
                  <FrameworkElement.Style>
                    <Style TargetType="{x:Type Grid}">
                      <Style.Triggers>
                        <DataTrigger Value="0" Binding="{Binding Text.Length, ElementName=AudioDownFilePath}">
                          <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                      </Style.Triggers>
                    </Style>
                  </FrameworkElement.Style>
                  <Button Style="{StaticResource ButtonStandardStyle}" Width="85" Margin="10,0,0,0" Content="Test" Click="AudioDownPlay_Click" />
                </Grid>
              </StackPanel>
            </StackPanel>
          </Grid>
          <Separator DockPanel.Dock="Top" Height="1" Background="#00FFFFFF" Margin="0,20,0,0" Visibility="{Binding IsChecked, ElementName=IsAudioDownAlertEnabled, Converter={StaticResource BooleanToVisibilityConverter}}" />
          <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,15,0,0">
            <CheckBox Name="IsAudioUpAlertEnabled" Content="Enable audio alert: host up" IsChecked="True" Click="IsAudioUpAlertEnabled_Click" />
          </StackPanel>
          <Grid DockPanel.Dock="Top" Margin="30,10,0,0" VerticalAlignment="Top" Visibility="{Binding IsChecked, ElementName=IsAudioUpAlertEnabled, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel>
              <TextBlock Text="Audio file path" />
              <TextBox Name="AudioUpFilePath" Margin="0,4,0,8" />
              <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <Button Style="{StaticResource ButtonStandardStyle}" Width="85" Content="Browse..." Click="AudioUpBrowse_Click" />
                <Grid>
                  <FrameworkElement.Style>
                    <Style TargetType="{x:Type Grid}">
                      <Style.Triggers>
                        <DataTrigger Value="0" Binding="{Binding Text.Length, ElementName=AudioUpFilePath}">
                          <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                      </Style.Triggers>
                    </Style>
                  </FrameworkElement.Style>
                  <Button Style="{StaticResource ButtonStandardStyle}" Width="85" Margin="10,0,0,0" Content="Test" Click="AudioUpPlay_Click" />
                </Grid>
              </StackPanel>
            </StackPanel>
          </Grid>
        </DockPanel>
      </TabItem>
      <TabItem Name="LogOutputTab" Header="Logging">
        <DockPanel Margin="20,0">
          <TextBlock DockPanel.Dock="Top" Text="Log Output" Style="{StaticResource OptionHeaderTextStyle}" />
          <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,15,0,0">
            <CheckBox Name="IsLogOutputEnabled" Content="Write ping output to plain text files" IsChecked="True" />
          </StackPanel>
          <Grid DockPanel.Dock="Top" Margin="30,10,0,0" VerticalAlignment="Top" Visibility="{Binding IsChecked, ElementName=IsLogOutputEnabled, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel>
              <TextBlock Text="Log path (base directory)" />
              <TextBox Name="LogPath" Margin="0,4,0,8" />
              <Button Style="{StaticResource ButtonStandardStyle}" Width="85" Content="Browse..." HorizontalAlignment="Left" Click="BrowseLogPath_Click" />
            </StackPanel>
          </Grid>
          <Separator DockPanel.Dock="Top" Height="1" Background="#00FFFFFF" Margin="0,20,0,0" Visibility="{Binding IsChecked, ElementName=IsLogOutputEnabled, Converter={StaticResource BooleanToVisibilityConverter}}" />
          <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,15,0,0">
            <CheckBox Name="IsLogStatusChangesEnabled" Content="Log status changes to text file" IsChecked="True" />
          </StackPanel>
          <Grid DockPanel.Dock="Top" Margin="30,10,0,0" VerticalAlignment="Top" Visibility="{Binding IsChecked, ElementName=IsLogStatusChangesEnabled, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel>
              <TextBlock Text="Log path" />
              <TextBox Name="LogStatusChangesPath" Margin="0,4,0,8" />
              <Button Style="{StaticResource ButtonStandardStyle}" Width="85" Content="Browse..." HorizontalAlignment="Left" Click="BrowseLogStatusChangesPath_Click" />
            </StackPanel>
          </Grid>
        </DockPanel>
      </TabItem>
      <TabItem Name="DisplayTab" Header="Display">
        <DockPanel Margin="20,0" LastChildFill="False">
          <TextBlock DockPanel.Dock="Top" Text="Display Options" Style="{StaticResource OptionHeaderTextStyle}" />
          <DockPanel Dock="Top" LastChildFill="False" Margin="0,15,0,0">
            <CheckBox Name="IsAlwaysOnTopEnabled" DockPanel.Dock="Left" Content="Always on top" />
          </DockPanel>
          <DockPanel Dock="Top" LastChildFill="False" Margin="0,15,0,0">
            <CheckBox Name="IsMinimizeToTrayEnabled" DockPanel.Dock="Left" Content="Minimize to tray" />
          </DockPanel>
          <DockPanel Dock="Top" LastChildFill="False" Margin="0,15,0,0">
            <CheckBox Name="IsExitToTrayEnabled" DockPanel.Dock="Left" Content="Exit to tray" />
          </DockPanel>
          <TextBlock DockPanel.Dock="Top" Text="Ping Screen" Margin="0,25,0,10" Style="{StaticResource OptionHeaderTextStyle}" />
          <TextBlock DockPanel.Dock="Top" Text="Choose what each host tile shows while a probe is running:" Margin="0,0,0,5" />
          <RadioButton Name="PingDisplayLogOption" DockPanel.Dock="Top" GroupName="PingDisplayMode" Content="Reply log only (the classic scrolling text output)" Margin="0,10,0,0" />
          <RadioButton Name="PingDisplayGraphOption" DockPanel.Dock="Top" GroupName="PingDisplayMode" Content="Latency graph only" Margin="0,8,0,0" />
          <RadioButton Name="PingDisplayBothOption" DockPanel.Dock="Top" GroupName="PingDisplayMode" Content="Both (default)" Margin="0,8,0,0" IsChecked="True" />
        </DockPanel>
      </TabItem>
      <TabItem Name="LayoutTab" Header="Colors">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
          <DockPanel Name="ColorsDockPanel" LastChildFill="False" Margin="20,0,20,20">
            <FrameworkElement.Resources>
              <ResourceDictionary>
                <Style x:Key="{x:Type StackPanel}" TargetType="{x:Type StackPanel}">
                  <Setter Property="StackPanel.Orientation" Value="Horizontal" />
                  <Setter Property="Margin" Value="0,5,0,0" />
                  <Setter Property="DockPanel.Dock" Value="Top" />
                </Style>
                <Style x:Key="{x:Type TextBox}" TargetType="{x:Type TextBox}" BasedOn="{StaticResource HtmlTextBoxStyle}">
                  <EventSetter Event="PreviewTextInput" Handler="HtmlColor_PreviewTextInput" />
                </Style>
                <Style x:Key="{x:Type Border}" TargetType="{x:Type Border}">
                  <Setter Property="BorderBrush" Value="#223028" />
                  <Setter Property="BorderThickness" Value="1" />
                  <Setter Property="Width" Value="80" />
                  <Setter Property="Margin" Value="15,0,0,0" />
                </Style>
              </ResourceDictionary>
            </FrameworkElement.Resources>
            <TextBlock DockPanel.Dock="Top" Text="Colors" Style="{StaticResource OptionHeaderTextStyle}" />
            <TextBlock DockPanel.Dock="Top" Text="Probe Background" Margin="0,10,0,0" Style="{StaticResource OptionSubHeaderTextStyle}" />
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,5,0,0">
              <TextBlock Width="150" Text="Inactive" />
              <TextBox Name="BackgroundColor_Probe_Inactive" />
              <Border Background="{Binding ElementName=BackgroundColor_Probe_Inactive, Path=Text, Converter={StaticResource StringToBrushConverter}}" />
            </StackPanel>
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,5,0,0">
              <TextBlock Width="150" Text="Up" />
              <TextBox Name="BackgroundColor_Probe_Up" />
              <Border Background="{Binding ElementName=BackgroundColor_Probe_Up, Path=Text, Converter={StaticResource StringToBrushConverter}}" />
            </StackPanel>
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,5,0,0">
              <TextBlock Width="150" Text="Down" />
              <TextBox Name="BackgroundColor_Probe_Down" />
              <Border Background="{Binding ElementName=BackgroundColor_Probe_Down, Path=Text, Converter={StaticResource StringToBrushConverter}}" />
            </StackPanel>
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,5,0,0">
              <TextBlock Width="150" Text="Indeterminate" />
              <TextBox Name="BackgroundColor_Probe_Indeterminate" />
              <Border Background="{Binding ElementName=BackgroundColor_Probe_Indeterminate, Path=Text, Converter={StaticResource StringToBrushConverter}}" />
            </StackPanel>
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,5,0,0">
              <TextBlock Width="150" Text="Error" />
              <TextBox Name="BackgroundColor_Probe_Error" />
              <Border Background="{Binding ElementName=BackgroundColor_Probe_Error, Path=Text, Converter={StaticResource StringToBrushConverter}}" />
            </StackPanel>
            <Separator DockPanel.Dock="Top" Height="1" Width="20" Background="#00FFFFFF" Margin="0,20,0,0" />
            <TextBlock DockPanel.Dock="Top" Text="Probe Foreground" Margin="0" Style="{StaticResource OptionSubHeaderTextStyle}" />
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,5,0,0">
              <TextBlock Width="150" Text="Inactive" />
              <TextBox Name="ForegroundColor_Probe_Inactive" />
              <Border Background="{Binding ElementName=ForegroundColor_Probe_Inactive, Path=Text, Converter={StaticResource StringToBrushConverter}}" />
            </StackPanel>
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,5,0,0">
              <TextBlock Width="150" Text="Up" />
              <TextBox Name="ForegroundColor_Probe_Up" />
              <Border Background="{Binding ElementName=ForegroundColor_Probe_Up, Path=Text, Converter={StaticResource StringToBrushConverter}}" />
            </StackPanel>
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,5,0,0">
              <TextBlock Width="150" Text="Down" />
              <TextBox Name="ForegroundColor_Probe_Down" />
              <Border Background="{Binding ElementName=ForegroundColor_Probe_Down, Path=Text, Converter={StaticResource StringToBrushConverter}}" />
            </StackPanel>
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,5,0,0">
              <TextBlock Width="150" Text="Indeterminate" />
              <TextBox Name="ForegroundColor_Probe_Indeterminate" />
              <Border Background="{Binding ElementName=ForegroundColor_Probe_Indeterminate, Path=Text, Converter={StaticResource StringToBrushConverter}}" />
            </StackPanel>
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,5,0,0">
              <TextBlock Width="150" Text="Error" />
              <TextBox Name="ForegroundColor_Probe_Error" />
              <Border Background="{Binding ElementName=ForegroundColor_Probe_Error, Path=Text, Converter={StaticResource StringToBrushConverter}}" />
            </StackPanel>
            <Separator DockPanel.Dock="Top" Height="1" Width="20" Background="#00FFFFFF" Margin="0,20,0,0" />
            <TextBlock DockPanel.Dock="Top" Text="Statistics Foreground" Margin="0" Style="{StaticResource OptionSubHeaderTextStyle}" />
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,5,0,0">
              <TextBlock Width="150" Text="Inactive" />
              <TextBox Name="ForegroundColor_Stats_Inactive" />
              <Border Background="{Binding ElementName=ForegroundColor_Stats_Inactive, Path=Text, Converter={StaticResource StringToBrushConverter}}" />
            </StackPanel>
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,5,0,0">
              <TextBlock Width="150" Text="Up" />
              <TextBox Name="ForegroundColor_Stats_Up" />
              <Border Background="{Binding ElementName=ForegroundColor_Stats_Up, Path=Text, Converter={StaticResource StringToBrushConverter}}" />
            </StackPanel>
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,5,0,0">
              <TextBlock Width="150" Text="Down" />
              <TextBox Name="ForegroundColor_Stats_Down" />
              <Border Background="{Binding ElementName=ForegroundColor_Stats_Down, Path=Text, Converter={StaticResource StringToBrushConverter}}" />
            </StackPanel>
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,5,0,0">
              <TextBlock Width="150" Text="Indeterminate" />
              <TextBox Name="ForegroundColor_Stats_Indeterminate" />
              <Border Background="{Binding ElementName=ForegroundColor_Stats_Indeterminate, Path=Text, Converter={StaticResource StringToBrushConverter}}" />
            </StackPanel>
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,5,0,10">
              <TextBlock Width="150" Text="Error" />
              <TextBox Name="ForegroundColor_Stats_Error" />
              <Border Background="{Binding ElementName=ForegroundColor_Stats_Error, Path=Text, Converter={StaticResource StringToBrushConverter}}" />
            </StackPanel>
            <Separator DockPanel.Dock="Top" Height="1" Width="20" Background="#00FFFFFF" Margin="0,20,0,0" />
            <TextBlock DockPanel.Dock="Top" Text="Alias Foreground" Margin="0" Style="{StaticResource OptionSubHeaderTextStyle}" />
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,5,0,0">
              <TextBlock Width="150" Text="Inactive" />
              <TextBox Name="ForegroundColor_Alias_Inactive" />
              <Border Background="{Binding ElementName=ForegroundColor_Alias_Inactive, Path=Text, Converter={StaticResource StringToBrushConverter}}" />
            </StackPanel>
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,5,0,0">
              <TextBlock Width="150" Text="Up" />
              <TextBox Name="ForegroundColor_Alias_Up" />
              <Border Background="{Binding ElementName=ForegroundColor_Alias_Up, Path=Text, Converter={StaticResource StringToBrushConverter}}" />
            </StackPanel>
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,5,0,0">
              <TextBlock Width="150" Text="Down" />
              <TextBox Name="ForegroundColor_Alias_Down" />
              <Border Background="{Binding ElementName=ForegroundColor_Alias_Down, Path=Text, Converter={StaticResource StringToBrushConverter}}" />
            </StackPanel>
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,5,0,0">
              <TextBlock Width="150" Text="Indeterminate" />
              <TextBox Name="ForegroundColor_Alias_Indeterminate" />
              <Border Background="{Binding ElementName=ForegroundColor_Alias_Indeterminate, Path=Text, Converter={StaticResource StringToBrushConverter}}" />
            </StackPanel>
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,5,0,10">
              <TextBlock Width="150" Text="Error" />
              <TextBox Name="ForegroundColor_Alias_Error" />
              <Border Background="{Binding ElementName=ForegroundColor_Alias_Error, Path=Text, Converter={StaticResource StringToBrushConverter}}" />
            </StackPanel>
            <Grid DockPanel.Dock="Top" Margin="0,15,0,0">
              <Button Style="{StaticResource ButtonStandardStyle}" Padding="15,5" Content="Restore Defaults" HorizontalAlignment="Left" Click="RestoreDefaultColors_Click" />
            </Grid>
          </DockPanel>
        </ScrollViewer>
      </TabItem>
    </TabControl>
    <Border Grid.Row="1" BorderBrush="#223028" Background="#17201a" BorderThickness="0,1,0,0">
      <DockPanel Margin="9" HorizontalAlignment="Right">
        <CheckBox Name="SaveAsDefaults" DockPanel.Dock="Top" Content="Save as DoMping defaults" FontSize="13" Foreground="#ffffff" HorizontalAlignment="Right" />
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" Margin="0,9,0,0" HorizontalAlignment="Right">
          <Button Style="{StaticResource ButtonStandardStyle}" Margin="0,0,9,0" IsDefault="True" Width="85" Content="OK" Click="OK_Click" />
          <Button Style="{StaticResource ButtonStandardStyle}" IsCancel="True" Width="85" Content="Cancel" />
        </StackPanel>
      </DockPanel>
    </Border>
  </Grid>
</Window>